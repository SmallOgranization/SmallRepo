
&НаКлиенте
Процедура РассчитатьСумму()
	
	// Выполняем рассчет
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	Если Не ТекущиеДанные = Неопределено Тогда
		ТекущиеДанные.Сумма = ТекущиеДанные.Цена * ТекущиеДанные.Количество;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыНоменклатураOnChange(Item)

	// Вычисляем суммы
	РассчитатьСумму();	

КонецПроцедуры

&НаКлиенте
Процедура ТоварыКоличествоOnChange(Item)
	
	// Вычисляем суммы
	РассчитатьСумму();

EndProcedure

